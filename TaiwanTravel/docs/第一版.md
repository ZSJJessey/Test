# 台湾 6 天 5 晚攻略站 — 建议与第一版改动清单

> 依据：PRD（`REQUIREMENTS.md`）、页面清单（`页面清单模块.md`）、当前静态实现（`index.html` / `script.js` / `style.css` / `assets`）对照整理。  
> 状态：**第一版已落地**（2026-05-16），高/中优先级项已完成；低优先级部分已做（打印、迷你目录、部分 P3–P6）。

---

## 1. 文档与项目治理

| 序号 | 项 | 说明 | 优先级 |
|------|----|------|--------|
| D1 | 新增 `README.md` | 说明本地如何打开、目录结构、GitHub Pages 部署步骤、内容维护入口（如 `台湾内容.md`） | 高 |
| D2 | 三处文案对齐 | `REQUIREMENTS.md` §9.1、`页面清单模块.md`、`index.html` 中 Hero 标题/副标题/按钮文案保持单一事实来源，改一处同步两处 | 中 |
| D3 | PRD 标注本期范围 | 对「Hero 淡入动效」「信息型图卡（图标卡片）」等待做项在 `REQUIREMENTS.md` 标注「本期不做 / 已延期」，避免验收歧义 | 中 |
| D4 | 变更记录 | 增加 `CHANGELOG.md` 或 README 内「版本说明」小节，记录重要内容与资源变更（如 Hero 图、Day 配图替换） | 低 |

---

## 2. 代码与资源一致性

| 序号 | 项 | 说明 | 优先级 |
|------|----|------|--------|
| C1 | 修正 `<link rel="preload">` | 当前预加载 `TaibeiStation.JPG` 与 `assets/image` 实际文件名不一致；改为与首屏首图或 LCP 关键图一致的真实路径（如实际使用的 `台北车站.JPG` 或 Hero 首张图），避免 404 与无效预加载 | 高 |
| C2 | Hero 首图与 `alt` 一致 | 首张 `src` 与 `alt` 描述需匹配（避免「象山」配图却写「101 夜景」类错位），兼顾无障碍与 SEO | 高 |
| C3 | 闲置图片策略 | 仓库中未在页面引用的图片（如旧版 Hero/Day 用图）：统一「保留备用」并记在 README，或移入 `assets/archive/` / 删除，避免资产混乱 | 中 |

---

## 3. 内容与行程产品（页面内可丰富）

| 序号 | 项 | 说明 | 优先级 |
|------|----|------|--------|
| P1 | 预算感区块 | 在总览或独立小节增加：人均总预算区间（是否含机票注明）、悠游卡/现金参考与「淡旺季浮动」一句；与正文已有数字呼应，不写死过时票价 | 中 |
| P2 | 关键交通可执行线索 | 日月潭↔阿里山、高铁、船班等：补充「建议提前多久订」「官方/平台检索关键词」占位，避免仅叙事无可查路径 | 中 |
| P3 | 住宿动线一句话 | 各住宿点补「区域 + 为何住这」（近捷运/码头/景区），不写具体房号级隐私 | 低 |
| P4 | 季节与装备 | 简短：最佳季节/雨季、阿里山温差与 Day5–6 衣物建议呼应 | 低 |
| P5 | 受众与合规 | 面向留学生等群体时，入境/证件一句「以出发前官方为准」，可留官方说明占位链接 | 低 |
| P6 | 美食卡片增强 | 可选：人均档位、排队策略、是否需订位（三选一即可） | 低 |

---

## 4. 体验与工程（不违背静态单页前提）

| 序号 | 项 | 说明 | 优先级 |
|------|----|------|--------|
| E1 | 打印 / PDF 友好 | `@media print`：隐藏轮播控件、可选默认展开时间线便于打印 | 低 |
| E2 | 页内迷你目录 | 长页顶部或首屏下方：锚点跳转「准备 / 时间线 / 路线 / 避坑 / 总结」 | 低 |
| E3 | 细粒度复制（可选） | 在「复制路线骨架」之外，按需增加「复制单日要点」等（需交互与文案设计） | 低 |

---

## 5. 与 PRD 第二阶段的关系

`REQUIREMENTS.md` §17 已列：真实地图嵌入、预算计算器、分享、多城市模板、小红书联动页等。

- [x] **地图（方案 1）**：`#route` 已嵌 Google iframe + 外链；节点与 `ROUTE_MAP_PLACES` 联动  
- [x] **预算计算器**：总览区 `#budgetCalc` 纯前端估算（不含机票）  
- **地图定位校正**：节点「九份」的 Google 关键词为 **台北车站**（非九份老街）；改 My Maps 时见 README「站点与地图定位对照」  
- **仍待做**：地图 JS API、分享增强、多城市模板等

---

## 6. 建议实施顺序（第一版落地）

1. **高**：C1、C2（修复预加载与图文一致，减少线上硬伤）  
2. **高**：D1（README，便于部署与交接）  
3. **中**：D2、C3、P1、P2  
4. **低**：其余文档标注、打印、迷你目录、CHANGELOG、P3–P6

---

## 7. 验收自检（第一版完成后可打勾）

- [x] 浏览器 Network 无关键资源 404（尤其 `preload` 与 Hero/Day 全部图片）  
- [x] 首屏首张图 `alt` 与画面内容一致  
- [x] README 含：打开方式、部署方式、主要文件说明  
- [x] Day 展开、准备 Tabs、路线节点联动、主题切换、复制路线仍正常  
- [x] 免责声明与政策表述仍为「以官方为准」，无过度承诺  
- [x] 路线区地图 iframe 可加载；点击节点可更新「打开当前站点」外链  
- [x] 预算计算器可展开并随输入更新人均/合计  

---

*文件版本：第一版+地图/计算器 · 与仓库内实现同步后请更新本清单勾选状态。*
